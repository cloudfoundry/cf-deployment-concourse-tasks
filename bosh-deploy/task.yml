---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/bosh-deploy

inputs:
- name: bbl-state
- name: cf-deployment
- name: cf-deployment-concourse-tasks
- name: ops-files
- name: vars-store

outputs:
- name: updated-vars-store

run:
  path: cf-deployment-concourse-tasks/bosh-deploy/task

params:
  BBL_STATE_DIR:
  MANIFEST_FILE: cf-deployment.yml
  OPS_FILES: opsfiles/gcp.yml
  SYSTEM_DOMAIN:
  VARS_STORE_PATH: deployment-vars.yml

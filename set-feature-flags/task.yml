---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/set-feature-flags

inputs:
- name: cf-deployment-concourse-tasks
- name: vars-store

params:
  VARS_STORE_FILE: deployment-vars.yml
  SYSTEM_DOMAIN:
  ENABLE_DIEGO_DOCKER: true
  ENABLE_TASK_CREATION: true

run:
  path: cf-deployment-concourse-tasks/set-feature-flags/task

---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/update-integration-configs

inputs:
- name: cf-deployment-concourse-tasks
- name: manifest-properties

outputs:
- name: updated-integration-config

run:
  path: cf-deployment-concourse-tasks/update-integration-configs/task

params:
  DEPLOYMENT_VARS_STORE:
